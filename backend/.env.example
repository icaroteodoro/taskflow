# ==============================================================================
# Taskflow Backend Environment Variables (Production)
# ==============================================================================
# Renomeie este arquivo para .env e preencha com os valores do seu ambiente de produção
# ==============================================================================

# Ativar o profile de produção
SPRING_PROFILES_ACTIVE=prod

# ==========================================
# Database Configuration
# ==========================================
DB_URL=jdbc:mysql://MEU-SERVIDOR-MYSQL:3306/taskflow_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
DB_USERNAME=usuario_producao
DB_PASSWORD=senha_super_secreta

# ==========================================
# JPA Configuration (Comportamento do Hibernate)
# ==========================================
# Use 'validate' ou 'none' em produção para evitar alterações acidentais na modelagem via código
DDL_AUTO=validate
SHOW_SQL=false

# ==========================================
# Security (JWT)
# ==========================================
# Gere uma chave longa e segura em produção (Base64 recomendado ou Hash longo)
JWT_SECRET=sua_chave_secreta_jwt_de_pelo_menos_256_bits_aqui_para_assinatura
# 1 Hora = 3600000ms
JWT_EXPIRATION_MS=3600000
# 24 Horas = 86400000ms / 7 Dias = 604800000ms
JWT_REFRESH_EXPIRATION_MS=604800000

# ==========================================
# SMTP / Mail Settings (Para Recuperação de Senhas e Verificação)
# ==========================================
MAIL_HOST=smtp.meuprovedor.com
MAIL_PORT=587
MAIL_USERNAME=no-reply@meusite.com
MAIL_PASSWORD=senha_do_email

# ==========================================
# Frontend Connection
# ==========================================
FRONTEND_URL=https://meutaskflow.com.br
