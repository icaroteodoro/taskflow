spring.application.name=taskflow

# Database Configuration (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/taskflow_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# JWT properties
jwt.secret=s9f8s9dfs8sdf7s8d7fs7df7s8d9f0s8df7s9df8s9d7f7s98dfs7f9s8dfs79f8s7df8s79fd
jwt.expirationMs=3600000 
jwt.refreshExpirationMs=86400000

# Mail / SMTP Server configuration
# Please inject these env variables before running:
# export MAIL_HOST=smtp.gmail.com
# export MAIL_PORT=587
# export MAIL_USERNAME=your_email@gmail.com
# export MAIL_PASSWORD=your_app_password
spring.mail.host=${MAIL_HOST:localhost}
spring.mail.port=${MAIL_PORT:1025}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend URL for emailing the links (Account verification, reset password)
app.frontend.url=http://localhost:3000
